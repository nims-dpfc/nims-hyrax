<%= f.input key,
            as: :multi_value,
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/local_terms?field=managing_organization_sim",
                      'autocomplete' => key }
            },
            required: f.object.required?(key) %>
