<h3>Deposits By Users (<%= @presenter.date_filter_string %>)</h3>
<ul>
  <% @presenter.depositors.each do |usr| %>
    <li>
      <%= link_to_profile(usr[:user]) %>
      deposited <%= pluralize(usr[:deposits], "file") %>
    </li>
  <% end %>
</ul>
