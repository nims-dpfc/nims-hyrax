<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type copy

  <store>
    @type stdout
  </store>

  <store>
    @type forward
    <server>
      host fluentd.example.jp
     port 24224
    </server>
  </store>
</match>
